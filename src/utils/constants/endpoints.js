// base url
const BASE_URL = `https://covid-fe-2023.vercel.app/api`;

const ENDPOINTS = {
  // endpoint indonesia.json
  INDONESIA: `${BASE_URL}/indonesia.json`,
  // endpoint global.json
  GLOBAL: `${BASE_URL}/global.json`,
};

export default ENDPOINTS;
